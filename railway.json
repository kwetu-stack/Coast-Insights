{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": [
      "FLASK_APP=app.py flask db upgrade && python seed_users.py && python import_july_sales.py && python import_trade_visits.py"
    ],
    "startCommand": "gunicorn app:app"
  }
}
